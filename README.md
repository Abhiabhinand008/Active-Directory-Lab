# INTRODUCTION

This project demonstrates a simulated attack on a self-built Active Directory (AD) environment. The lab was configured using a Windows Server domain controller with multiple users and service accounts to replicate a real-world enterprise network.

The objective was to identify and exploit common vulnerabilities such as weak passwords and misconfigured service accounts, leading to full domain compromise.
